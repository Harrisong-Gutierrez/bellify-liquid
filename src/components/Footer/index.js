import PropTypes from "prop-types";
import FooterList from "../FooterList";

function Footer() {
  return (
    <footer>
      <FooterList />
    </footer>
  );
}

Footer.propTypes = {};

export default Footer;
